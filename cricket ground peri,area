#include <stdio.h>

int main() {
   int length,breadth;
   scanf("%d %d",&length,&breadth);
   int perimeter=2*(length+breadth);
   int area=length*breadth;
   printf("%d\n",perimeter);
   printf("%d\n",area);

    
    return 0;
}
